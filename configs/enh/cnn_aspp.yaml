project:
  name: CNN_ASPP_ENH
  experiment_name: CNN_ASPP_ENH
data:
  input_mode: enh
  input_dir: ./data/Input/ENH/
  target_dir: ./data/Target/
  fold_csv: ./data/Input/all_patches_with_folds.csv
  norm_param_dir: ./data/Norm_parameters/ENH/
  norm_files:
    min: min.npy
    max: max.npy
    mean: mean.npy
    std: std.npy
  normalization: minmax
train:
  n_folds: 5
  epochs: 500
  batch_size: 8
  learning_rate: 0.0001
  weight_decay: 0.01
  max_grad_norm: 2.0
  patience: 200
  num_workers: 4
  seed: 1004
  amp: true
model:
  arch: cnn_aspp
  in_channels: 1
  dropout_rate: 0.5
  atrous_rates:
  - 3
  - 6
  - 12
  - 18
  activation: leakyrelu
  negative_slope: 0.01
logging:
  use_wandb: true
  save_root: ./outputs
